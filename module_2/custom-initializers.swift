// initializers are functions that are called when a new instance of a struct or class is created

struct Player {
    let name: String
    let number: Int

    init(name: String) {
        self.name = name
        number = Int.random(in: 1...99)
    }
}